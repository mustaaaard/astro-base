---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE } from "../config/site.mjs";
---

<Layout
	title={`404 - Lost in the Void | ${SITE.title}`}
	description="This page doesn't exist. But you do, and that's pretty cool."
	canonicalURL={`${SITE.url}/404`}
>
	<Header variant="editorial" />
	<main
		id="main-content"
		role="main"
		class="px-4 py-16 text-center h-[75vh] flex justify-center items-center"
	>
		<div class="max-w-2xl mx-auto">
			<p class="text-9xl font-display font-bold text-gray-500 mb-4">404</p>
			<h1
				class="font-display text-4xl md:text-5xl font-bold text-gray-900 mb-4"
			>
				Well, this is awkward.
			</h1>
			<p class="text-xl md:text-2xl text-gray-600 mb-8">
				You&apos;ve wandered into the void. This page either packed its bags and
				left, never existed in the first place, or is hiding from you
				specifically.
			</p>
			<p class="text-lg text-gray-500 mb-8">
				Don&apos;t worry, even the best explorers get lost sometimes.
			</p>
			<a
				href="/"
				class="inline-block px-8 py-3 bg-black text-white hover:bg-gray-800 transition-colors font-medium"
			>
				Take me home
			</a>
		</div>
	</main>
	<Footer />
</Layout>
